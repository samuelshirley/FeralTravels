node_modules/
.expo/
dist/
web-build/
expo-env.d.ts

# Native build artifacts (CNG/prebuild — regenerated by EAS)
ios/
# The .app the local iOS e2e loop installs on the simulator. `ios/` above
# already covers the derived data it is copied FROM; this is the copy
# scripts/ios-e2e-local.sh keeps so `run` can re-install without rebuilding.
ios-build/
android/

# Env
.env*.local

# macOS
.DS_Store
