node_modules/
.next/
out/
build/
tmp/

# database / data
db/
src/data/gpx/*
!src/data/gpx/.gitkeep

# env — never commit any .env* variants holding secrets.
# .env.example IS tracked (it's the template with empty values) and is
# explicitly re-included on the last line.
.env
.env.*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# editor / OS
.DS_Store
*.log
.idea/
.vscode/
.claude/
