// DEPRECATED: MoreStopsModal has been removed. This file can be safely deleted.
import { describe, it, expect } from 'vitest';

describe('MoreStopsModal (deprecated)', () => {
  it('is a no-op', () => {
    expect(true).toBe(true);
  });
});
