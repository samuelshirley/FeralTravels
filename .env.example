# ---- Anthropic (Penny / chat replanning) ----
ANTHROPIC_API_KEY=sk-ant-...

# ---- Google Maps + Places (one key for both) ----
# Used client-side by TripMap (Maps JS API) and server-side by the fuel-stop
# planner (Places API New). Enable both "Maps JavaScript API" and
# "Places API (New)" on this key in Google Cloud Console.
# MUST have the NEXT_PUBLIC_ prefix — Next.js only exposes env vars to
# the browser if they start with NEXT_PUBLIC_.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=AIza...

# ---- Postgres (Neon) ----
# Create a free project at https://neon.tech, then copy the pooled connection string.
# Use the pooled URL (sslmode=require) for DATABASE_URL.
DATABASE_URL=postgres://user:pass@ep-xxx-pooler.region.aws.neon.tech/dbname?sslmode=require

# ---- Auth.js (NextAuth v5) ----
# Generate with:  openssl rand -base64 32
AUTH_SECRET=replace-me-with-a-random-32-byte-base64-string
# Public origin (set this in production; localhost is fine for dev)
AUTH_URL=http://localhost:3000

# ---- Google OAuth provider ----
# Create at https://console.cloud.google.com → APIs & Services → Credentials → OAuth client ID
# Authorized redirect URI: http://localhost:3000/api/auth/callback/google
AUTH_GOOGLE_ID=...apps.googleusercontent.com
AUTH_GOOGLE_SECRET=GOCSPX-...

# ---- Resend (email magic link) ----
# Get an API key at https://resend.com/api-keys
AUTH_RESEND_KEY=re_...
# IMPORTANT: `onboarding@resend.dev` only sends to the email that owns the
# Resend account — every other recipient gets a 403 and the magic-link sign-in
# fails with a 500. Use a verified sender (your own domain) OR a free
# `*.resend.dev` subdomain claimed in the Resend dashboard
# (Domains → Add Domain → resend.dev subdomain). Example:
#   AUTH_EMAIL_FROM=noreply@trip-planner.resend.dev
AUTH_EMAIL_FROM=onboarding@resend.dev

# ---- Admin dashboard ----
# Optional restriction. Must be a SUBSET of the hardcoded ADMIN_ALLOWLIST in
# src/server/auth/admin.ts — setting an arbitrary email here cannot grant admin.
# Defense in depth: even with this env, the user must also have a verified
# email and the silent is_admin DB flag set at sign-in.
ADMIN_EMAILS=samuelashirley@gmail.com

# ---- Cost / abuse guardrails for Penny (Anthropic) ----
# Per-user request cap per rolling hour. Beyond this, replans 429 until it cools off.
REPLAN_REQUESTS_PER_HOUR=40
# Per-user spend cap per rolling 24h, in USD (estimated from Anthropic list pricing).
REPLAN_USD_CAP_PER_DAY=5
